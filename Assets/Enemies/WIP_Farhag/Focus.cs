/*using LevelState;
using System.Collections;
using UnityEngine;

public class Focus : MonoBehaviour
{

    // Use this for initialization
    void Start(RoomState room)
    {
        room.addFoci(this);
    }

    // Update is called once per frame
}*/
