/*using EnemyImplements.All.Implements;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Enemies
{
    public class Gremlisus : Enemy
    {
        // Start is called before the first frame update
        public Gremlisus(int maxHP) : base()
        {
            damageable = new ClassicalHP(maxHP, this);
        }
        void Start()
        {
        }

        // Update is called once per frame
        protected override void Update()
        {

        }
    }
}*/
